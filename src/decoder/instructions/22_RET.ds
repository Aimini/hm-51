#0x22 RET
RF(SP), ALU(A), SR(WE)
RF(PCH, WE), BUS(RAM) # PCH, WE <- STACK[SP]
RF(SP, WE), ALU(DEC), SR(WE)
RF(PCL, WE), BUS(RAM) # PCL, SE <- STACK[SP]
RF(SP, WE), ALU(DEC), J(STAGE_CHECK_INTERRUPT_SKIP_PF)
