########### 0x75, MOV direct, #immed

RF(T0,WE), BUS(ROM),BR(ONE)              # T0 <- direct

@INC "snippet\inc_pc_wrsr.ds"
BUS(ROM), WR(WE)         # WR <- immed

RF(T0),   ALU(SFR), RFSRCR(WE)
RF(T0),   ALU(A),   SR(WE)
RF(SFR),  ALU(B),  RAM(WE), BR(ONE) # RAM[direct] <- immed

@INC "snippet\inc_pc_jchkint.ds"