# 0x10 JB bit, offset
@INC "instructions\get_bit_byte_ends_with.ds", "BR(Q)"

RF(T1), ALU(EXTB), BR(CY)  # 
JBIT(ADDC_PC_BEFORE_CHECK_INTERRUPT), WR(WE), LI(1), BR(ONE, CPLQ) #PC + 1(WR) + 1(BR)

@INC "snippet\inc_pc_jsjmp.ds"
