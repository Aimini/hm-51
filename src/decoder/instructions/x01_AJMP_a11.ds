# 8'bXXX0_0000 AJMP  addr11
RF(PCL, WE), ALU(INCC), BR(CY, CPLQ), JALUNF(AJMP_SKIP_PCH)   # PCL + 1,  STORE CARRY
RF(PCH, WE), ALU(INCC)           # PCH + CY
AJMP_SKIP_PCH:
RF(PCL, WE), BUS(ROM), SR(WE)
RF(IR), ALU(SWAP), WR(WE)
RF(PCH, WE), ALU(ADDR11REPLACE),WR(WE)
RF(IR,WE),BUS(ROM),  J(STAGE_CHECK_INTERRUPT_SKIP_LOAD_IR)
