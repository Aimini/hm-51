# 8'bXXX0_0000 AJMP  addr11
BR(ONE)
RF(PCL, WE), ALU(INCC), BR(CY)   # PCL + 1,  STORE CARRY
RF(PCH, WE), ALU(INCC)           # PCH + CY

RF(PCL, WE), BUS(ROM)
RF(IR), ALU(SWAP), WR(WE)
RF(PCH, WE), ALU(ADDR11REPLACE), J(STAGE_CHECK_INTERRUPT)
